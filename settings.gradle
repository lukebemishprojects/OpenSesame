rootProject.name = 'opensesame'

def subprojects = [
        'annotations',
        'runtime',
        'groovy'
]

include subprojects

subprojects.each {
    project(":$it").name = "opensesame-$it"
}
