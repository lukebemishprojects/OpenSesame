rootProject.name = 'opensesame'

def subprojects = [
        'annotations',
        'runtime',
        'compile',
        'groovy',
        'javac'
]

include subprojects

subprojects.each {
    project(":$it").name = "opensesame-$it"
}
