rootProject.name = 'opensesame'

def subprojects = [
        'annotations',
        'runtime',
        'compile',
        'groovy'
]

include subprojects

subprojects.each {
    project(":$it").name = "opensesame-$it"
}
